
require("config")
require("shared-data")

require("prototypes.label")
require("prototypes.targeters")

data:extend({
  {
    type = "custom-input",
    name = "toggle-ability-mode",
    key_sequence = "CONTROL + ALT + a",
  },
})